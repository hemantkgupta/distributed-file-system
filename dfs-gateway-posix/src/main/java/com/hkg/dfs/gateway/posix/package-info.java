/**
 * POSIX protocol gateway for the DFS cluster — NFSv3 (primary) and Kubernetes CSI (secondary).
 *
 * <p>See {@code SPEC.md} in this module's root for the implementation contract.
 * Maps to <strong>§8 POSIX Gateway</strong> in the service catalog.
 *
 * <p>Status: spec only — implementation has not yet been generated against {@code SPEC.md}.
 * SMB is explicitly out of scope for the initial pass.
 */
package com.hkg.dfs.gateway.posix;
